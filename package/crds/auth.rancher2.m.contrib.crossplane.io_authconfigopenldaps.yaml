---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: authconfigopenldaps.auth.rancher2.m.contrib.crossplane.io
spec:
  group: auth.rancher2.m.contrib.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - rancher2
    kind: AuthConfigOpenldap
    listKind: AuthConfigOpenldapList
    plural: authconfigopenldaps
    singular: authconfigopenldap
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthConfigOpenldap is the Schema for the AuthConfigOpenldaps
          API. Provides a Rancher v2 Auth Config OpenLdap resource. This can be used
          to configure and enable Auth Config OpenLdap for Rancher v2 RKE clusters
          and retrieve their information.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AuthConfigOpenldapSpec defines the desired state of AuthConfigOpenldap
            properties:
              forProvider:
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: openldap_user://<DN>  openldap_group://<DN>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  certificateSecretRef:
                    description: Base64 encoded CA certificate for TLS if self-signed.
                      Use filebase64() for encoding file (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  connectionTimeout:
                    description: OpenLdap connection timeout. Default 5000 (int)
                    type: number
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  groupDnAttribute:
                    description: Group DN attribute. Default entryDN (string)
                    type: string
                  groupMemberMappingAttribute:
                    description: Group member mapping attribute. Default member (string)
                    type: string
                  groupMemberUserAttribute:
                    description: Group member user attribute. Default entryDN (string)
                    type: string
                  groupNameAttribute:
                    description: Group name attribute. Default cn (string)
                    type: string
                  groupObjectClass:
                    description: Group object class. Default groupOfNames (string)
                    type: string
                  groupSearchAttribute:
                    description: Group search attribute. Default cn (string)
                    type: string
                  groupSearchBase:
                    description: Group search base (string)
                    type: string
                  groupSearchFilter:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  nestedGroupMembershipEnabled:
                    description: Nested group membership enable. Default false (bool)
                    type: boolean
                  port:
                    description: OpenLdap port. Default 389 (int)
                    type: number
                  servers:
                    description: OpenLdap servers list (list)
                    items:
                      type: string
                    type: array
                  serviceAccountDistinguishedNameSecretRef:
                    description: Service account DN for access OpenLdap service (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  serviceAccountPasswordSecretRef:
                    description: Service account password for access OpenLdap service
                      (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  startTls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  testPasswordSecretRef:
                    description: Password for test access to OpenLdap service (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  testUsername:
                    description: Username for test access to OpenLdap service (string)
                    type: string
                  tls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  userDisabledBitMask:
                    description: User disabled bit mask (int)
                    type: number
                  userEnabledAttribute:
                    description: User enable attribute (string)
                    type: string
                  userLoginAttribute:
                    description: User login attribute. Default uid (string)
                    type: string
                  userMemberAttribute:
                    description: User member attribute. Default memberOf (string)
                    type: string
                  userNameAttribute:
                    description: User name attribute. Default givenName (string)
                    type: string
                  userObjectClass:
                    description: User object class. Default inetorgperson (string)
                    type: string
                  userSearchAttribute:
                    description: User search attribute. Default uid|sn|givenName (string)
                    type: string
                  userSearchBase:
                    description: User search base DN (string)
                    type: string
                  userSearchFilter:
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: openldap_user://<DN>  openldap_group://<DN>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  certificateSecretRef:
                    description: Base64 encoded CA certificate for TLS if self-signed.
                      Use filebase64() for encoding file (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  connectionTimeout:
                    description: OpenLdap connection timeout. Default 5000 (int)
                    type: number
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  groupDnAttribute:
                    description: Group DN attribute. Default entryDN (string)
                    type: string
                  groupMemberMappingAttribute:
                    description: Group member mapping attribute. Default member (string)
                    type: string
                  groupMemberUserAttribute:
                    description: Group member user attribute. Default entryDN (string)
                    type: string
                  groupNameAttribute:
                    description: Group name attribute. Default cn (string)
                    type: string
                  groupObjectClass:
                    description: Group object class. Default groupOfNames (string)
                    type: string
                  groupSearchAttribute:
                    description: Group search attribute. Default cn (string)
                    type: string
                  groupSearchBase:
                    description: Group search base (string)
                    type: string
                  groupSearchFilter:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  nestedGroupMembershipEnabled:
                    description: Nested group membership enable. Default false (bool)
                    type: boolean
                  port:
                    description: OpenLdap port. Default 389 (int)
                    type: number
                  servers:
                    description: OpenLdap servers list (list)
                    items:
                      type: string
                    type: array
                  serviceAccountDistinguishedNameSecretRef:
                    description: Service account DN for access OpenLdap service (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  serviceAccountPasswordSecretRef:
                    description: Service account password for access OpenLdap service
                      (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  startTls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  testPasswordSecretRef:
                    description: Password for test access to OpenLdap service (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  testUsername:
                    description: Username for test access to OpenLdap service (string)
                    type: string
                  tls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  userDisabledBitMask:
                    description: User disabled bit mask (int)
                    type: number
                  userEnabledAttribute:
                    description: User enable attribute (string)
                    type: string
                  userLoginAttribute:
                    description: User login attribute. Default uid (string)
                    type: string
                  userMemberAttribute:
                    description: User member attribute. Default memberOf (string)
                    type: string
                  userNameAttribute:
                    description: User name attribute. Default givenName (string)
                    type: string
                  userObjectClass:
                    description: User object class. Default inetorgperson (string)
                    type: string
                  userSearchAttribute:
                    description: User search attribute. Default uid|sn|givenName (string)
                    type: string
                  userSearchBase:
                    description: User search base DN (string)
                    type: string
                  userSearchFilter:
                    type: string
                required:
                - serviceAccountDistinguishedNameSecretRef
                - serviceAccountPasswordSecretRef
                - testPasswordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.servers is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.servers)
                || (has(self.initProvider) && has(self.initProvider.servers))'
            - message: spec.forProvider.serviceAccountDistinguishedNameSecretRef is
                a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountDistinguishedNameSecretRef)'
            - message: spec.forProvider.serviceAccountPasswordSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceAccountPasswordSecretRef)'
            - message: spec.forProvider.testPasswordSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.testPasswordSecretRef)'
            - message: spec.forProvider.testUsername is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.testUsername)
                || (has(self.initProvider) && has(self.initProvider.testUsername))'
            - message: spec.forProvider.userSearchBase is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userSearchBase)
                || (has(self.initProvider) && has(self.initProvider.userSearchBase))'
          status:
            description: AuthConfigOpenldapStatus defines the observed state of AuthConfigOpenldap.
            properties:
              atProvider:
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: openldap_user://<DN>  openldap_group://<DN>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  connectionTimeout:
                    description: OpenLdap connection timeout. Default 5000 (int)
                    type: number
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  groupDnAttribute:
                    description: Group DN attribute. Default entryDN (string)
                    type: string
                  groupMemberMappingAttribute:
                    description: Group member mapping attribute. Default member (string)
                    type: string
                  groupMemberUserAttribute:
                    description: Group member user attribute. Default entryDN (string)
                    type: string
                  groupNameAttribute:
                    description: Group name attribute. Default cn (string)
                    type: string
                  groupObjectClass:
                    description: Group object class. Default groupOfNames (string)
                    type: string
                  groupSearchAttribute:
                    description: Group search attribute. Default cn (string)
                    type: string
                  groupSearchBase:
                    description: Group search base (string)
                    type: string
                  groupSearchFilter:
                    type: string
                  id:
                    description: (Computed) The ID of the resource (string)
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: (Computed) The name of the resource (string)
                    type: string
                  nestedGroupMembershipEnabled:
                    description: Nested group membership enable. Default false (bool)
                    type: boolean
                  port:
                    description: OpenLdap port. Default 389 (int)
                    type: number
                  servers:
                    description: OpenLdap servers list (list)
                    items:
                      type: string
                    type: array
                  startTls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  testUsername:
                    description: Username for test access to OpenLdap service (string)
                    type: string
                  tls:
                    description: Enable TLS connection (bool)
                    type: boolean
                  type:
                    description: (Computed) The type of the resource (string)
                    type: string
                  userDisabledBitMask:
                    description: User disabled bit mask (int)
                    type: number
                  userEnabledAttribute:
                    description: User enable attribute (string)
                    type: string
                  userLoginAttribute:
                    description: User login attribute. Default uid (string)
                    type: string
                  userMemberAttribute:
                    description: User member attribute. Default memberOf (string)
                    type: string
                  userNameAttribute:
                    description: User name attribute. Default givenName (string)
                    type: string
                  userObjectClass:
                    description: User object class. Default inetorgperson (string)
                    type: string
                  userSearchAttribute:
                    description: User search attribute. Default uid|sn|givenName (string)
                    type: string
                  userSearchBase:
                    description: User search base DN (string)
                    type: string
                  userSearchFilter:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
