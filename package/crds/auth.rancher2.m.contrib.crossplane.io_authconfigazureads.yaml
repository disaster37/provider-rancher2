---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: authconfigazureads.auth.rancher2.m.contrib.crossplane.io
spec:
  group: auth.rancher2.m.contrib.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - rancher2
    kind: AuthConfigAzuread
    listKind: AuthConfigAzureadList
    plural: authconfigazureads
    singular: authconfigazuread
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthConfigAzuread is the Schema for the AuthConfigAzureads API.
          Provides a Rancher v2 Auth Config AzureAD resource. This can be used to
          configure and enable Auth Config AzureAD for Rancher v2 RKE clusters and
          retrieve their information.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AuthConfigAzureadSpec defines the desired state of AuthConfigAzuread
            properties:
              forProvider:
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: azuread_user://<USER_ID>  azuread_group://<GROUP_ID>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  applicationIdSecretRef:
                    description: AzureAD auth application ID (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  applicationSecretSecretRef:
                    description: AzureAD auth application secret (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authEndpoint:
                    description: AzureAD auth endpoint (string)
                    type: string
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  endpoint:
                    description: AzureAD endpoint. Default https://login.microsoftonline.com/
                      (string)
                    type: string
                  graphEndpoint:
                    description: AzureAD graph endpoint (string)
                    type: string
                  groupMembershipFilter:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  rancherUrl:
                    description: Rancher URL (string). "<rancher_url>/verify-auth-azure"
                    type: string
                  tenantId:
                    description: AzureAD tenant ID (string)
                    type: string
                  tokenEndpoint:
                    description: AzureAD token endpoint (string)
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: azuread_user://<USER_ID>  azuread_group://<GROUP_ID>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  applicationIdSecretRef:
                    description: AzureAD auth application ID (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  applicationSecretSecretRef:
                    description: AzureAD auth application secret (string)
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  authEndpoint:
                    description: AzureAD auth endpoint (string)
                    type: string
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  endpoint:
                    description: AzureAD endpoint. Default https://login.microsoftonline.com/
                      (string)
                    type: string
                  graphEndpoint:
                    description: AzureAD graph endpoint (string)
                    type: string
                  groupMembershipFilter:
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  rancherUrl:
                    description: Rancher URL (string). "<rancher_url>/verify-auth-azure"
                    type: string
                  tenantId:
                    description: AzureAD tenant ID (string)
                    type: string
                  tokenEndpoint:
                    description: AzureAD token endpoint (string)
                    type: string
                required:
                - applicationIdSecretRef
                - applicationSecretSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.applicationIdSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.applicationIdSecretRef)'
            - message: spec.forProvider.applicationSecretSecretRef is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.applicationSecretSecretRef)'
            - message: spec.forProvider.authEndpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.authEndpoint)
                || (has(self.initProvider) && has(self.initProvider.authEndpoint))'
            - message: spec.forProvider.graphEndpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.graphEndpoint)
                || (has(self.initProvider) && has(self.initProvider.graphEndpoint))'
            - message: spec.forProvider.rancherUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.rancherUrl)
                || (has(self.initProvider) && has(self.initProvider.rancherUrl))'
            - message: spec.forProvider.tenantId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tenantId)
                || (has(self.initProvider) && has(self.initProvider.tenantId))'
            - message: spec.forProvider.tokenEndpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tokenEndpoint)
                || (has(self.initProvider) && has(self.initProvider.tokenEndpoint))'
          status:
            description: AuthConfigAzureadStatus defines the observed state of AuthConfigAzuread.
            properties:
              atProvider:
                properties:
                  accessMode:
                    description: Access mode for auth. required, restricted, unrestricted
                      are supported. Default unrestricted (string)
                    type: string
                  allowedPrincipalIds:
                    description: 'Allowed principal ids for auth. Required if access_mode
                      is required or restricted. Ex: azuread_user://<USER_ID>  azuread_group://<GROUP_ID>
                      (list)'
                    items:
                      type: string
                    type: array
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      Annotations of the resource (map)
                      Annotations of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  authEndpoint:
                    description: AzureAD auth endpoint (string)
                    type: string
                  enabled:
                    description: Enable auth config provider. Default true (bool)
                    type: boolean
                  endpoint:
                    description: AzureAD endpoint. Default https://login.microsoftonline.com/
                      (string)
                    type: string
                  graphEndpoint:
                    description: AzureAD graph endpoint (string)
                    type: string
                  groupMembershipFilter:
                    type: string
                  id:
                    description: (Computed) The ID of the resource (string)
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels of the resource (map)
                      Labels of the resource
                    type: object
                    x-kubernetes-map-type: granular
                  name:
                    description: (Computed) The name of the resource (string)
                    type: string
                  rancherUrl:
                    description: Rancher URL (string). "<rancher_url>/verify-auth-azure"
                    type: string
                  tenantId:
                    description: AzureAD tenant ID (string)
                    type: string
                  tokenEndpoint:
                    description: AzureAD token endpoint (string)
                    type: string
                  type:
                    description: (Computed) The type of the resource (string)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
