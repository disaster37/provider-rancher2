apiVersion: rancher2.contrib.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  credentials:
    secretRef:
      key: credentials
      name: rancher-default
      namespace: default
    source: Secret